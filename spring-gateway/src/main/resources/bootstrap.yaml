spring:
  application:
    name: spring-gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        prefix: ${spring.application.name}
        file-extension: yaml
        namespace: public
        group: DEFAULT_GROUP
  profiles:
    active: dev

